run:
  name: dummy
  seed: 42

data:
  manifest: data/manifests/dummy.csv

encoder:
  name: dummy
  options: {}

protocol:
  kind: linear_probe
  task: multiclass

output:
  embeddings_dir: artifacts/embeddings
  runs_dir: artifacts/runs